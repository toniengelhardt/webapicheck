export default defineEventHandler(async () => {
  // const webApis = useWebApiList().value
  // console.log('webApis', webApis)
  return [] /* Object.keys(webApiData).map(apiId => ({
    loc: `/apis/${apiId}`,
    priority: 1,
  })) */
})
